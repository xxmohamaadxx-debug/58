# إعداد اتصال Neon Database

## المشكلة الحالية

يظهر خطأ `password authentication failed for user 'neondb_owner'` مما يعني أن رابط الاتصال غير صحيح أو منتهي الصلاحية.

## الحل

### 1. الحصول على رابط الاتصال الصحيح من Neon Dashboard

1. اذهب إلى [Neon Console](https://console.neon.tech/)
2. اختر المشروع الخاص بك
3. اذهب إلى **Dashboard** > **Connection Details**
4. اختر **Connection pooling** أو **Direct connection**
5. انسخ رابط الاتصال (Connection String)

**مثال على رابط الاتصال:**
```
postgresql://username:password@ep-example-123456.us-east-1.aws.neon.tech/neondb?sslmode=require
```

### 2. إضافة رابط الاتصال في ملف .env

أنشئ ملف `.env` في مجلد المشروع (إذا لم يكن موجوداً):

```bash
# في مجلد المشروع
touch .env
```

أضف رابط الاتصال في الملف:

```env
VITE_NEON_DATABASE_URL=postgresql://username:password@host/database?sslmode=require
```

**⚠️ مهم:**
- استبدل `username:password@host/database` برابط الاتصال الصحيح من Neon Dashboard
- لا تضيف مسافات حول علامة `=`
- تأكد من أن رابط الاتصال كامل ومباشر من Neon Dashboard

### 3. إعادة تشغيل الخادم

بعد إضافة ملف `.env`:

```bash
# أوقف الخادم (Ctrl+C)
# ثم أعد تشغيله
npm run dev
```

### 4. التحقق من الإعداد

افتح Console المتصفح (F12) وتحقق من:
- عدم وجود رسائل خطأ حول `VITE_NEON_DATABASE_URL`
- عمل تسجيل الدخول بشكل صحيح

## استكشاف الأخطاء

### الخطأ: "password authentication failed"

**السبب:** رابط الاتصال غير صحيح أو منتهي الصلاحية

**الحل:**
1. احصل على رابط جديد من Neon Dashboard
2. تأكد من نسخ الرابط كاملاً بدون تعديل
3. تأكد من وجود ملف `.env` في المجلد الصحيح
4. أعد تشغيل الخادم

### الخطأ: "VITE_NEON_DATABASE_URL غير موجود"

**السبب:** ملف `.env` غير موجود أو المتغير غير مضاف

**الحل:**
1. تأكد من وجود ملف `.env` في جذر المشروع
2. تأكد من تنسيق الملف الصحيح:
   ```
   VITE_NEON_DATABASE_URL=...
   ```
3. لا تضيف علامات اقتباس حول القيمة
4. أعد تشغيل الخادم

### الخطأ: "Cannot read property 'sql' of null"

**السبب:** قاعدة البيانات غير متصلة

**الحل:**
1. تحقق من رابط الاتصال في Neon Dashboard
2. تأكد من أن قاعدة البيانات نشطة
3. تحقق من ملف `.env`

## ملاحظات مهمة

- ⚠️ **لا تشارك ملف `.env`** - أضفه إلى `.gitignore`
- 🔒 رابط الاتصال يحتوي على كلمة المرور - احفظه بأمان
- 🔄 إذا انتهت صلاحية رابط الاتصال، احصل على رابط جديد من Neon Dashboard
- ✅ استخدم رابط **Connection pooling** للأداء الأفضل

## رابط مفيد

- [Neon Documentation](https://neon.tech/docs)
- [Connection Strings Guide](https://neon.tech/docs/connect/connection-string)
